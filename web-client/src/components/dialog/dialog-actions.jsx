import React from 'react';
import { DialogActions as MaterialDialogActions } from '@material-ui/core';

export const DialogActions = ({ children }) => (
	<MaterialDialogActions>{children}</MaterialDialogActions>
);
